import { ResCuisinesPipe } from './res-cuisines.pipe';

describe('ResCuisinesPipe', () => {
  it('create an instance', () => {
    const pipe = new ResCuisinesPipe();
    expect(pipe).toBeTruthy();
  });
});
